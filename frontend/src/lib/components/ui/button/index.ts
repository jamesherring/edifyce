import Root, {
	type ButtonVariant,
	type ButtonSize,
	type ButtonProps,
	buttonVariants
} from './button.svelte';

export {
	Root,
	Root as Button,
	buttonVariants,
	type ButtonVariant,
	type ButtonSize,
	type ButtonProps
};
