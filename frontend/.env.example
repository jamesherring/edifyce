# Base URL of the Edifyce FastAPI backend.
#
# Defaults to http://localhost:8000 (the local uvicorn address) when unset.
# Set to an empty value for same-origin requests when the built bundle is
# served by FastAPI itself.
VITE_API_BASE_URL=http://localhost:8000
