# Edifyce FastAPI backend
#
# The application currently reads no environment variables — it exposes a
# stateless compile/verify API with no database, auth, or external services.
# This file is kept as a placeholder for future configuration.
