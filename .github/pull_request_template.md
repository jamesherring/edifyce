<!-- Everything above "Changes by area" is the summary: aim for under ~400 words
     and language a new contributor would follow. Depth is welcome — it goes in
     "Design decisions" and "Implementation details", below the fold. -->

## In one paragraph

<!-- Plain language. What was missing or broken, and what can now be done that
     couldn't be before. Avoid terms this repo invented unless you define them. -->

## Where this sits

- **Roadmap:** <!-- §4.6 of docs/some-roadmap.md — or "none" -->
- **Builds on:** <!-- #204, #206 — or "nothing" -->
- **Unblocks:** <!-- what this makes possible next — or "nothing queued" -->

## Simplest example

<!-- The smallest before/after that shows the change: a request and response, a
     proof fragment, three lines of code. Shortest thing that makes the point. -->

<details>
<summary>A harder case, if it clarifies by comparison</summary>

</details>

## Changes by area

<!-- Delete rows that don't apply — except Kernel, which always says "none" when
     untouched, so its absence never has to be guessed at. -->

| Area | What changed |
|---|---|
| **Kernel** — `website/logical/kernel/` | none |
| **Proof engine** — rest of `website/logical/` | |
| **Database** — `app/db/`, `migrations/` | |
| **API** — `app/routers/`, `app/schemas.py` | |
| **Frontend — UI** — components, routes, styling | |
| **Frontend — other** — `api.ts`, stores, types | |
| **Tests** | |
| **Docs** — `docs/`, `AGENTS.md` | |

## Checks

- [ ] `uv run pytest` — <!-- N passed -->
- [ ] `npm run check` and `npm run test`
- [ ] Migration generated by `atlas migrate diff`, with `atlas.sum` committed — or n/a

---

## Design decisions

<!-- The choices a reviewer would otherwise have to reverse-engineer: why this
     shape, what was rejected and why, what the change deliberately is not.
     Write as much as it takes — this section has no budget. -->

## Implementation details

<!-- Mechanics, measurements, edge cases, migration notes. Put review-round
     history in the collapsed block below rather than in the summary: it is how
     the PR got here, not what it does. -->

<details>
<summary>Review rounds</summary>

</details>
