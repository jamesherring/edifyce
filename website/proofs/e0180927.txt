
let $\alpha$, $\beta$, $\gamma$ be formula

# First three axioms the same as in propositional logic

$(\alpha \rightarrow (\beta \rightarrow \alpha))$ label{Ax: A1}

$((\alpha \rightarrow (\beta \rightarrow \gamma)) \rightarrow ((\alpha \rightarrow \beta) \rightarrow (\alpha \rightarrow \gamma)))$ label{Ax: A2}

$((\neg \beta \rightarrow \neg \alpha) \rightarrow (\alpha \rightarrow \beta))$ label{Ax: A3}





let $x$, $y$, $z$ be variable
let $t$ be term


# May consider separating the conditions so that one can map and the other evaluate to True

if $\beta$ == $\alpha$.replace($x$, $t$, Condition($x$.free_in($\alpha$))) and $t$.is_free_for($x$, $\alpha$):

    $(\forall x \; \alpha \rightarrow \beta)$ label{Ax: A4}



if $x$ not in $\alpha$.free_variables():

    $(\forall x \; (\alpha \rightarrow \beta) \rightarrow (\alpha \rightarrow \forall x \; \beta))$ label{Ax: A5}


$\forall x \; x = x$ label{Ax: A6}




let $\varphi$, $\psi$ be atomic_formula


if $\varphi$.equivalent_with_some_replacements($\psi$, $x$, $y$):

    $(x = y \rightarrow (\varphi \rightarrow \psi))$ label{Ax: A7}














