
let $\alpha$, $\beta$, $\gamma$, $a$, $b$ be formula

# First three axioms the same as in propositional logic

$(\alpha \rightarrow (\beta \rightarrow \alpha))$ label{Ax: A1}

$((\alpha \rightarrow (\beta \rightarrow \gamma)) \rightarrow ((\alpha \rightarrow \beta) \rightarrow (\alpha \rightarrow \gamma)))$ label{Ax: A2}

$((\neg \beta \rightarrow \neg \alpha) \rightarrow (\alpha \rightarrow \beta))$ label{Ax: A3}





let $x$, $y$, $z$ be variable
let $t$ be term

$(\forall x \; a \rightarrow b)$ label{Ax: A4.a}


if $\alpha$ == $\beta$:

    $(\forall x \; \alpha \rightarrow \beta)$ label{Ax: A4.0}

if $t$.is_free_for($x$, $\alpha$):

    $(\forall x \; \alpha \rightarrow \beta)$ label{Ax: A4.1}

 
if $b$ == $a$.replace($x$, $t$, $x$.free()):

    $(\forall x \; a \rightarrow b)$ label{Ax: A4.2}


if $t$.is_free_for($x$, $\alpha$) and $\beta$ == $\alpha$.replace($x$, $t$, $x$.free()):

    $(\forall x \; \alpha \rightarrow \beta)$ label{Ax: A4}









# if $x$ not in $\alpha$.free_variables:

    # $(\forall x \; (\alpha \rightarrow \beta) \rightarrow (\alpha \rightarrow \forall x \; \beta))$ ref{A5}


# $\forall x \; x = x$ label{Ax: A6}



# let $\alpha$, $\beta$ be atomic_formula


# if $\alpha$.replace_equivalent($\beta$, $x$, $y$):

    # $(x = y \rightarrow (\alpha \rightarrow \beta))$ ref{A7}


# $(\forall x \; \forall y \; x = y \rightarrow \forall y \; x = y[y/y])$ ref{A4}


# Inference Rules

# from $\Gamma \vdash \alpha$ and $\Gamma \vdash (\alpha \rightarrow \beta)$ deduce $\Gamma \vdash\beta$
