import axioms as ax


let $A$, $B$, $\alpha$, $\beta$, $\gamma$, $\delta$ be formula


if $\alpha$:
    $(\alpha \rightarrow (\beta \rightarrow \alpha))$ label{Ax: A1}


if $\gamma$:
    $(\gamma \rightarrow (\delta \rightarrow \gamma))$ ref{Th, A1}



if $\neg \alpha$:

    # $(\neg \beta \rightarrow \neg \alpha)$ ref{DT1}

    # $((\neg \beta \rightarrow \neg \alpha) \rightarrow (\alpha \rightarrow \beta))$ ref{ax.A3}

    # $(\alpha \rightarrow \beta)$ ref{MP}

# $(\neg \alpha \rightarrow (\alpha \rightarrow \beta))$ ref{DT2}



let $\gamma$, $\delta$ be formula
let $x$, $y$ be variable

# $(\forall x \; \gamma \rightarrow \delta)$ ref{Th, ax.A4.0}

