
let $\alpha$, $\beta$ be formula

$(\neg \alpha \rightarrow (\neg \beta \rightarrow \neg \alpha))$ ref{A1} label{A}

if $\neg \alpha$:

    $(\neg \beta \rightarrow \neg \alpha)$ ref{DT1, A} label{Y}

    $((\neg \beta \rightarrow \neg \alpha) \rightarrow (\alpha \rightarrow \beta))$ ref{A3}

    $(\alpha \rightarrow \beta)$ ref{MP, 8, 10}

$(\neg \alpha \rightarrow (\alpha \rightarrow \beta))$ ref{DT2, 12}


