import axioms as ax


let $\alpha$, $\beta$ be formula

$(\neg \alpha \rightarrow (\neg \beta \rightarrow \neg \alpha))$ ref{ax.A1}

if $\neg \alpha$:

    $(\neg \beta \rightarrow \neg \alpha)$ ref{DT1}

    $((\neg \beta \rightarrow \neg \alpha) \rightarrow (\alpha \rightarrow \beta))$ ref{ax.A3}

    $(\alpha \rightarrow \beta)$ ref{MP}

$(\neg \alpha \rightarrow (\alpha \rightarrow \beta))$ ref{DT2}



let $\gamma$, $\delta$ be formula

$(\neg \gamma \rightarrow (\gamma \rightarrow (\delta \rightarrow \delta)))$ ref{Th}


if $\gamma$, $\delta$:
    if $\neg \gamma$:
        $(\neg \gamma \rightarrow (\gamma \rightarrow (\delta \rightarrow \delta)))$ ref{T}
        
        $\neg \gamma$ ref{As}
        
        $(\gamma \rightarrow (\delta \rightarrow \delta))$ ref{MP}
        
        $\gamma$ ref{As}
        
        $(\delta \rightarrow \delta)$ ref{MP}
        
        $\delta$ ref{As}
        
        $\delta$ ref{MP}
        
    
