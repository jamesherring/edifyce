import axioms as ax



let $\gamma$, $\delta$ be formula
let $x$, $y$, $a$, $b$ be variable
let $t_2$, $t_3$ be term


if $\delta$ == $\gamma$.replace($y$, $t_2$, Condition($y$.free_in($\gamma$))):
    if $t_2$.is_free_for($y$, $\gamma$):
    
        $(\forall y \; \gamma \rightarrow \delta)$ ref{Th, ax.A4}
    

#$(\forall x \; x = a \rightarrow y = a)$ ref{Th, ax.A4, $t \mapsto y$}
    
#$(\forall y \; (a = b \rightarrow a = x) \rightarrow (a = b \rightarrow \forall y \; a = x))$ ref{Th, ax.A5}

#$\forall y \; y = y$ ref{Th, ax.A6}

$(x = y \rightarrow (x = y \rightarrow y = y))$ ref{Th, ax.A7}



#let $A$, $B$, $\alpha$, $\beta$, $\gamma$, $\delta$ be formula

#define $(A \lor B)$ as $(\neg A \rightarrow B)$ for formula label{or}


#$(\neg \alpha \rightarrow (\neg \beta \rightarrow \neg \alpha))$ ref{Th, ax.A1} label{X}

#$(\neg \alpha \rightarrow (\beta \lor \neg \alpha))$ ref{Def, X, or}

#if $\neg \alpha$:

    #$(\neg \beta \rightarrow \neg \alpha)$ ref{DT1, X}

    #$((\neg \beta \rightarrow \neg \alpha) \rightarrow (\alpha \rightarrow \beta))$ ref{Th, ax.A3}

    #$(\alpha \rightarrow \beta)$ ref{MP}

#$(\neg \alpha \rightarrow (\alpha \rightarrow \beta))$ ref{DT2}

