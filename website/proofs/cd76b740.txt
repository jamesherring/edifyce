
import deduce_anything_from_a_contradiction as res

let $\alpha$, $\beta$, $\gamma$, $\delta$ be formula

define $(\alpha \land \beta)$ as $\neg (\alpha \rightarrow \neg \beta)$

if $\alpha$:
    
    $\neg (\alpha \rightarrow \neg \beta)$ ref{A1}

if $(\gamma \rightarrow \gamma)$:

    $((\gamma \rightarrow \gamma) \land \delta)$ ref{T, 10}

